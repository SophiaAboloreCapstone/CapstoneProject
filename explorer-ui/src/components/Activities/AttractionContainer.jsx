import * as React from "react"
export default function AttractionContainer({name, image, description, source}) {
    return (
        <div className="attraction-container">
            
        </div>
    )
}
